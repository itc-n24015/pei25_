day = ['月', '月', '火', '水', '木', '金', '金']

.## 重複している曜日を削除（2回目の「月」と「金」をremoveで削除）
day.remove('月')  # 最初に見つかった「月」を削除（重複分をなくす）
day.remove('金')  # 最初に見つかった「金」を削除（重複分をなくす）

.## '土' と '日' を末尾に追加（insertを使って順番を指定）
day.insert(len(day), '土')  # リストの最後に「土」を追加
day.insert(len(day), '日')  # リストの最後に「日」を追加

.## 結果を出力
print(day) 
